# ClaudeKey TODOs

Generated by /plan-ceo-review + /plan-eng-review on 2026-03-28

## P1 - Before Hardware Assembly

### 提取公共 Swift 模块
创建 app/shared/ 目录, 把 SpeechEngine (~100行) 和其他共享逻辑提取出来。
修改 build.sh 支持多文件编译 (swiftc app/shared/*.swift app/lite/ClaudeKeyLite.swift)。
**Effort:** S (CC: ~30 min) | **Depends on:** nothing
**Source:** Eng review DRY issue (2026-03-28)

### Terminal 前台检测 (Safety Critical)
在 Accept/Reject/Up/Down 发送前检查前台 app 是否为 Terminal/iTerm。
使用 `NSWorkspace.shared.frontmostApplication.bundleIdentifier`。
Auto-Accept 必须在此功能完成后才能启用。
**Effort:** S (CC: ~15 min) | **Depends on:** 公共模块提取

### GPIO 冲突修复 + Pro 固件集成
- 编码器从 GPIO1/2 改为 GPIO38/39 (CLK/DT), GPIO3 保留 (SW)
- OLED 保持 GPIO1/2 (I2C SDA/SCL)
- main.cpp 集成 Pro 模块: displayInit/Update + encoderInit/Read + Pro 额外按键
- CLAUDE.md 显示方案统一为 SSD1309 2.42" OLED (I2C), 编码器引脚更新
**Effort:** M (CC: ~30 min) | **Depends on:** nothing

### Serial 协议统一
- 固件 main.cpp: S: → L: (LED颜色), M: → (合并到 L:), 新增 D: (OLED显示数据)
- host 端 (macOS app): 新增串口连接 + 写入逻辑 (~100行 Swift), 读取 /tmp/claudekey-status.json 后发送 L:/D: 命令到 ESP32
- 文档已用 L:/D:/A:/K:/E: 前缀, 固件对齐即可
**Effort:** S (CC: ~30 min) | **Depends on:** nothing

### 非阻塞蜂鸣器
把 beep() 从 delay() 阻塞改为 millis-based state machine。
当前 beepReject() 阻塞 180ms, beepPttStart() 阻塞 90ms, 期间按键扫描暂停。
**Effort:** S (CC: ~15 min) | **Depends on:** nothing
**Source:** Eng review performance issue (2026-03-28)

## P2 - Before Open Source Release

### 会话级路由
Terminal 前台检测只能区分 app, 不能区分同一 Terminal 中的多个 tab/session。
需要绑定到具体的 Claude Code 会话 (通过 hooks 的 session_id 或 PID 跟踪)。
**Effort:** L (CC: ~3 hours) | **Depends on:** Terminal 前台检测
**Source:** Codex outside voice (CEO review 2026-03-28)

### 测试套件
当前零测试。开源发布前需要基本的 Swift 单元测试覆盖:
- Terminal 检测逻辑
- Serial 协议解析
- PTT 状态机
- Auto-Accept 安全限制
**Effort:** M (CC: ~1 hour) | **Depends on:** 功能实现完成

### DESIGN.md 创建
运行 /design-consultation 创建设计系统文档。包含:
- 色彩系统 (LED 状态色 + app 背景/前景/强调色)
- 字体规范 (macOS app + OLED)
- 间距尺度
- 动画语言 (呼吸/脉冲/闪烁的参数)
- 状态视觉映射 (idle/working/approval/error/disconnected)
统一 macOS app + 菜单栏 app + 硬件 LED/OLED 的视觉语言。
**Effort:** M (CC: ~30 min) | **Depends on:** nothing
**Source:** Design review (2026-03-28)

### "未连接"状态设计
固件默认是绿色呼吸 LED, 用户可能误以为一切正常但实际 host 未连接。
添加: LED 缓慢白色呼吸 + OLED "Waiting for host..." 作为未连接状态。
收到第一条 Serial 命令后切换到正常状态。
**Effort:** S (CC: ~15 min) | **Depends on:** Serial 协议统一
**Source:** Design review (2026-03-28)

### macOS app 状态色带
面板顶部加一条颜色条, 和硬件 LED 灯带同步。
用户余光就能看到 Claude 的状态 (绿=空闲, 蓝=工作, 黄=注意, 红=危险, 紫=录音)。
**Effort:** S (CC: ~15 min) | **Depends on:** nothing
**Source:** Design review (2026-03-28)

### OLED Compact/Detail 双模式
默认 Compact (2 行: context bar + status), 旋钮切换到 Detail (5 行全部信息)。
128x64 单色屏上少即是多。
**Effort:** S (CC: ~30 min) | **Depends on:** Pro 固件集成
**Source:** Design review (2026-03-28)

## Deferred (from CEO + Design Review)

### 固件 OTA 更新 (WiFi)
WiFi 配置体验需要更多设计思考。当前用 USB 更新即可。

### 定制键帽 + 图标
先验证按键布局不会再变, 再投入定制键帽生产。

### macOS App 签名 + 公证
Homebrew Cask 分发需要 Apple Developer 签名和公证流程。
需要 Apple Developer Program ($99/year)。
